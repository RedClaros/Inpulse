module.exports = {
  getClients: () => {
    return []; // returns an empty array for now
  },

  syncClients: () => {
    console.log('syncClients() called — placeholder logic');
    return true;
  }
};
